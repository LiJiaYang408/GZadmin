spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demo?useSSL=false&serverTimezone=UTC
    username: root
    password: DeepReads123!

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

mybatis:
  mapper-locations: classpath:mappers/*.xml  # Mapper XML 文件的路径
  type-aliases-package: com.example.demo.model  # 实体类的包路径
  configuration:
    map-underscore-to-camel-case: true  # 自动映射下划线到驼峰命名